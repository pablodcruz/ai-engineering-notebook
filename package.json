{
  "name": "ai-engineering-notebook-browser-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Isolated browser tests for the Support Triage Review Console.",
  "scripts": {
    "test:browser": "playwright test",
    "test:browser:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
